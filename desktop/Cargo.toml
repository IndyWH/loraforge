# Workspace for the desktop shell. `sidecar` is the process-lifecycle library
# (no Tauri dependency — testable without webkit); `src-tauri` is the thin
# Tauri app (needs webkit2gtk dev packages to build on Linux, see CLAUDE.md).
[workspace]
resolver = "2"
members = ["sidecar", "src-tauri"]
